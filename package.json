{
  "name": "aws-node-express-dynamodb-api-project",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.1181.0",
    "dynamoose": "^1.11.1",
    "express": "^4.17.1",
    "hateoas": "^1.0.0",
    "serverless-http": "^2.7.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "serverless-dynamodb-local": "^0.2.40",
    "serverless-offline": "^9.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "IS_OFFLINE=true && ENV=dev && npx nodemon --exec sls offline start --host 0.0.0.0"
  }
}
